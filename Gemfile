source 'https://rubygems.org'

group :development do
    gem 'ZenTest'  # for autotest

    # only for local dev...to remove os notification,
    # run `touch .NO_TEST_NOTIFIER` from shell
    gem 'test_notifier'
end
